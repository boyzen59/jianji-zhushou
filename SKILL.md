---
name: jian-ji-zhu-shou-2-0
description: 为中文历史长视频制作分镜、录制需求与原声切片、程序化动效和带REV关联的可编辑PPT审阅稿。支持素材全量编排、中文回修与分轨交付；Word修订稿仅含中文。
---

# 历史视频剪辑助手 3.1

保留原调用名。按当前请求读取相关参考，不因调用Skill而自动执行整片制作、安装、发布或监控。

## 内容与范围

- 最新用户指令优先。正文决定叙事，批准的屏显修订决定画面文字；英文参考稿用于术语和语义配对。三者分别保存，不以旧草稿覆盖人工修改；“已批准”不等于缺失原稿已恢复。
- 从具体文案的论点、因果与证据写分镜，再选素材、版式与运动。不增加事实、假数字或重复内容凑镜头；除明确要求外，字号、节奏、构图和具体动效由内容决定。
- 项目路径、素材数量、旧REV修改、实拍映射、精确小窗坐标及检查时间写入项目配置/执行手册，不固化为下一项目的要求。分镜表、审阅稿和视频共用场景数据。
- 本用户提供的AI图片、AI视频默认全部编入；历史图片只选与文案相关者，宁可不用也不误用。实际库存作为全用分母，不能写死300/200。素材不存在、损坏或无法合理对应正文时列明具体问题，不以误用、隐藏素材或占位假完成。详见[素材规则](references/assets-and-history.md)。

## 固定画面规则

- 不生成旁白字幕、浮动字幕、底部字幕框或默认SRT/ASS交付。已有SRT仅作时间输入，保留源文件。标题、论点、史料说明和图表标注仍可出现，屏显中英语义1:1；中文修订Word不含英文。
- 不显示装饰性类别/时长/镜号：Duration label、CHAPTER、2 SECONDS、HOLD/DUR、INTRO、CH01及其装饰线、右上角镜号胶囊、人像录制静音提示。历史年份、真实标题、图表关系线保留；REV、时码放备注/独立索引。
- 文字卡片采用淡黑透明玻璃、白字、柔和阴影，无白色外圈或内圈。浮动信息框优先左上；史料为框内大图与独立侧栏。审阅页和视频均不放空媒体槽、空人像圈、缺图标志或“素材待补”。详见[视觉规则](references/visual-style.md)。
- 真人仅在大章开头前10秒全屏、后5秒收为小窗；真实小章开头用小窗；全片最后结尾可全屏。其他正文不出镜。录制引导才有3秒静音＋蜂鸣，正常时间线不加。详见[声音与出镜](references/audio-render-delivery.md)。
- 章节之间统一[固定2秒卡](references/3秒双语标题卡_制作规则.md)，不再叠加旧1.5秒间隔。普通相邻分镜的主动作、卡片结构不重复，同一内容优先合并；固定章卡在不同章节边界复用是例外，同一边界不连续播两张。
- 模板数字只能来自用户提供的资料，柱顶数字默认不显示；无数据用定性关系图，不沿用示例值、虚构比例或用柱高暗示数量。

## 按需工作路由

| 当前任务 | 读取与执行 |
|---|---|
| 素材整理、全量编排、历史图选用 | [assets-and-history.md](references/assets-and-history.md)；库存、语义匹配、实际可见区间 |
| 分镜与动效文案 | [v2版式](references/v2-templates.md)＋[动效路由](references/video-shotcraft-routing.md)；参考[分镜模板](assets/templates/video-spec-template.md)，按实际模块实现 |
| 玻璃、史料布局、真人小窗 | [visual-style.md](references/visual-style.md)；样式起点[card.css](assets/templates/card.css) |
| 录制需求、切片、声音、成片 | [audio-render-delivery.md](references/audio-render-delivery.md)；先解决真正需要补录的区间 |
| PPT/中文Word审阅及回修 | [qa-and-schema.md](references/qa-and-schema.md)；默认PPTX，Word按需中文单语，PDF仅在用户明确要求时另出 |

Remotion占全部分镜>75%；动效镜中Shotcraft占85%–100%，RVE与Scenes合计0%–10%，其余原生/自定义，三者合计100%。引擎、版式与动效来源分别记录，详见动效路由；比例不代替语义选择。

## 安装、验证与续跑

- 首次安装用 scripts/bootstrap_motion_libraries.py --install-missing 检查/安装Shotcraft、RVE、Scenes及v2源码，均在外部缓存。Skill只保留精简规则、可用模板、参考PDF与许可；不再嵌入上游完整源码。
- 开始制作前做与当前阶段有关的一次自检，缺什么补什么。scripts/preflight_check.py检查入口/依赖存在性，不等于证明所有模块可调用或所有素材已覆盖；首次使用再实际查询模块、检查目标编译入口。依赖版本遵从现有锁文件，不默认升级。
- HyperFrames是CLI包，检查其package.json的bin和命令帮助，不用普通模块主入口误判缺包。依赖恢复不自动触发旧工程prepare/预览脚本；必要的原生构建仅针对具体缺项。文档/媒体运行时优先复用已安装的可用环境。
- 首轮按请求一次交付完整内容；用户要样张时才出样张。不默认逐图观看、逐页截图或反复文字自查；只检查数据、资源、帧边界、对象映射和退出码，视觉与措辞由用户审阅。
- 输入未变复用解析、翻译、探测和通过的渲染段；输入变更只更新直接依赖项。检查点保存当前阶段、已完成范围、失败原因与真实续跑入口，不混淆“已分配”“已输出”“用户已通过”。
- 续跑先确认进程与检查点，不重建计划清空进度；需要重建时先备份。未要求定时则不创建监控；有监控只在完成、失败或需要用户介入时通知，不强制无变化进度播报。

可复用脚本：query_motion_library.py（模块查询）、build_presenter_audio.py（录制切片）、build_ai_video_audio_stem.py（AI原声轨）、build_render_plan.py（分段计划）、watch_render_progress.py（进度快照）、verify_master_media.py（母版流参数）。调用参数以各脚本--help为准；没有真实产物不报告完成。
